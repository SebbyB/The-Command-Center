import type { PageMeta } from '../page-types';

export const meta: PageMeta = {
  name: 'realtime',
  description: 'WebSocket data receiver. Connect to any WebSocket server — an ESP32, a web server, or anything else — and watch data stream in.',
};

export const readme = `# realtime

${meta.description}
`;
