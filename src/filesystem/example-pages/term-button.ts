import type { PageMeta } from '../page-types';

export const meta: PageMeta = {
  name: 'term-button',
  description: 'Buttons that run terminal commands. Click any button to execute the command in the terminal below.',
};

export const readme = `# term-button

${meta.description}
`;
